name: JafarApp
options:
  bundleIdPrefix: ru.jafar
  deploymentTarget:
    iOS: "17.0"
    macOS: "14.0"
settings:
  base:
    SWIFT_VERSION: "5.10"
targets:
  JafarApp:
    type: application
    platform: [iOS, macOS]
    sources:
      - path: JafarApp
    info:
      path: JafarApp/Info.plist
      properties:
        CFBundleDisplayName: "JAFAR"
        CFBundleShortVersionString: "2.0.0"
        CFBundleVersion: "1"
        LSMinimumSystemVersion: "14.0"
        NSMicrophoneUsageDescription: "Джафар использует микрофон для голосовых команд."
        NSSpeechRecognitionUsageDescription: "Джафар использует распознавание речи для выполнения голосовых команд."
        JAFARCommandEndpoint: "$(JAFAR_COMMAND_ENDPOINT)"
        JAFARUserId: "$(JAFAR_USER_ID)"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ru.jafar.legal-ai
        PRODUCT_NAME: Jafar
        ENABLE_HARDENED_RUNTIME: "YES"
